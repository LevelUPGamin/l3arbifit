import { Layout } from '@/components/layout/Layout';
import PageContent from '@/components/PageContent';

export default function Privacy() {
  return (
    <Layout>
      <PageContent slug="privacy" className="container mx-auto px-4 py-12" />
    </Layout>
  );
}
