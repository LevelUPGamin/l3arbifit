import { Layout } from '@/components/layout/Layout';
import PageContent from '@/components/PageContent';

export default function Terms() {
  return (
    <Layout>
      <PageContent slug="terms" className="container mx-auto px-4 py-12" />
    </Layout>
  );
}
